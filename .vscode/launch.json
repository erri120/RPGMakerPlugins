{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-msedge",
      "request": "launch",
      "name": "Launch Edge against localhost",
      "url": "http://localhost:5500/projects/example/index.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}